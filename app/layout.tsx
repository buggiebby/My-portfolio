import './globals.css';
import type { Metadata } from 'next';
import { Inter, Playfair_Display } from 'next/font/google';

const inter = Inter({ subsets: ['latin'], variable: '--font-inter' });
const playfair = Playfair_Display({ subsets: ['latin'], variable: '--font-playfair' });

export const metadata: Metadata = {
  title: 'Buggie Restaurant - Fine Dining Restaurant',
  description: 'Experience exceptional cuisine and warm hospitality at Bella Vista, where culinary artistry meets fine dining in the heart of the city.',
  keywords: 'restaurant, fine dining, cuisine, food, reservations, gourmet',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.variable} ${playfair.variable} font-sans`}>
        {children}
      </body>
    </html>
  );
}